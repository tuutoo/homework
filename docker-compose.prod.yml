services:
  app:
    image: tuutoo/homework:latest
    container_name: homework_app
    ports:
      - "5173:80"
      - "8000:8000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
